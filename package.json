{
  "name": "ordle",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run",
    "check:leak": "node scripts/check-leak.mjs",
    "dictionary": "node scripts/build-dictionary.mjs",
    "verify": "vitest run && nuxt build && node scripts/check-leak.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "nuxt": "^4.5.2",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vitest": "^2.1.0"
  }
}
